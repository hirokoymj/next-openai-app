import { Suspense } from 'react';
import BlogList from '@/components/BlogList';

//==Server component
const page = () => {
  return (
    <div>
      <h1>Blog</h1>
      <Suspense fallback={<p>...Loading</p>}>
        <BlogList />
      </Suspense>
    </div>
  );
};

export default page;
